services:
  switchdeck:
    image: techblog/switchdeck:latest
    ports:
      - "8080:8080"
    volumes:
      - switchdeck-data:/data/switchdeck
    environment:
      LOG_LEVEL: info
    restart: unless-stopped
    network_mode: host

volumes:
  switchdeck-data:
